## Notes (files removed without content copy)

The following pre-SSOT duplicates were removed from `src/conditions/` without copying their full contents here because they were **redundant** and are available canonically at:

- `src/conditions/dimension-to-signal-mapping.json`

Rationale:
- The repo now treats `src/conditions/**` as canonical for condition data.
- Keeping large duplicate JSON blobs inside `src/conditions/` increases the risk of divergence.

