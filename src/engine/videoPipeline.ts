export function createVideoSummary() {
  return { scope: "video", status: "ready" };
}

// current lane: video
export function videoTask() {
  return { scope: "video", status: "ready" };
}

// forced-video-2
