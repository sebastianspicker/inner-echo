/**
 * Safe Mode: maximum intensity allowed when Safe Mode is on.
 * Clamps effect strength so users are not overwhelmed.
 */
export const SAFE_MODE_INTENSITY_MAX = 0.7

/** Safe Mode: max temporal smear feedback (blend with previous frame). */
export const SAFE_MODE_TEMPORAL_FEEDBACK_MAX = 0.25

/** Safe Mode: max temporal smear jitter (pixel offset). */
export const SAFE_MODE_TEMPORAL_JITTER_MAX = 0.06
