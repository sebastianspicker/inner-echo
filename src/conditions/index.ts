export function createConditionsSummary() {
  return { scope: "conditions", status: "ready" };
}

// current lane: conditions
export function conditionsTask() {
  return { scope: "conditions", status: "ready" };
}
