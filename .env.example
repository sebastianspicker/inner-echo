# No environment variables are required for the app itself.
# The values below are optional shell overrides for local smoke/test scripts.

HOST=127.0.0.1
PORT=5173
HEADLESS=1

# Set to 1 only when intentionally requiring local browser probes to exercise
# audio and microphone paths.
REQUIRE_AUDIO=0
REQUIRE_MIC=0
